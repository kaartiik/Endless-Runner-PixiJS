export function GROUP1(grid) {
  const coinsArr = [];

  const coin1 = {
    x: grid.matrix[4][5].x,
    y: grid.matrix[4][5].y,
  };

  coinsArr.push(coin1);

  const coin2 = {
    x: grid.matrix[5][5].x,
    y: grid.matrix[5][5].y,
  };

  coinsArr.push(coin2);

  const coin3 = {
    x: grid.matrix[6][5].x,
    y: grid.matrix[6][5].y,
  };

  coinsArr.push(coin3);

  const coin4 = {
    x: grid.matrix[7][5].x,
    y: grid.matrix[7][5].y,
  };

  coinsArr.push(coin4);

  const coin5 = {
    x: grid.matrix[4][6].x,
    y: grid.matrix[4][6].y,
  };

  coinsArr.push(coin5);

  const coin6 = {
    x: grid.matrix[6][6].x,
    y: grid.matrix[6][6].y,
  };

  coinsArr.push(coin6);

  const coin7 = {
    x: grid.matrix[4][7].x,
    y: grid.matrix[4][7].y,
  };

  coinsArr.push(coin7);

  const coin8 = {
    x: grid.matrix[5][7].x,
    y: grid.matrix[5][7].y,
  };

  coinsArr.push(coin8);

  const coin9 = {
    x: grid.matrix[6][7].x,
    y: grid.matrix[6][7].y,
  };

  coinsArr.push(coin9);

  const coin10 = {
    x: grid.matrix[7][7].x,
    y: grid.matrix[7][7].y,
  };

  coinsArr.push(coin10);

  const coin11 = {
    x: grid.matrix[4][8].x,
    y: grid.matrix[4][8].y,
  };

  coinsArr.push(coin11);

  const coin12 = {
    x: grid.matrix[5][8].x,
    y: grid.matrix[5][8].y,
  };

  coinsArr.push(coin12);

  const coin13 = {
    x: grid.matrix[6][8].x,
    y: grid.matrix[6][8].y,
  };

  coinsArr.push(coin13);

  const coin14 = {
    x: grid.matrix[7][8].x,
    y: grid.matrix[7][8].y,
  };

  coinsArr.push(coin14);

  const coin15 = {
    x: grid.matrix[7][9].x,
    y: grid.matrix[7][9].y,
  };

  coinsArr.push(coin15);

  const coin16 = {
    x: grid.matrix[7][10].x,
    y: grid.matrix[7][10].y,
  };

  coinsArr.push(coin16);

  const coin17 = {
    x: grid.matrix[4][11].x,
    y: grid.matrix[4][11].y,
  };

  coinsArr.push(coin17);

  const coin18 = {
    x: grid.matrix[7][11].x,
    y: grid.matrix[7][11].y,
  };

  coinsArr.push(coin18);

  const coin19 = {
    x: grid.matrix[4][12].x,
    y: grid.matrix[4][12].y,
  };

  coinsArr.push(coin19);

  const coin20 = {
    x: grid.matrix[5][12].x,
    y: grid.matrix[5][12].y,
  };

  coinsArr.push(coin20);

  const coin21 = {
    x: grid.matrix[6][12].x,
    y: grid.matrix[6][12].y,
  };

  coinsArr.push(coin21);

  const coin22 = {
    x: grid.matrix[7][12].x,
    y: grid.matrix[7][12].y,
  };

  coinsArr.push(coin22);

  const coin23 = {
    x: grid.matrix[4][13].x,
    y: grid.matrix[4][13].y,
  };

  coinsArr.push(coin23);

  const coin24 = {
    x: grid.matrix[7][13].x,
    y: grid.matrix[7][13].y,
  };

  coinsArr.push(coin24);

  return coinsArr;
}

export function GROUP2(grid) {
  const coinsArr = [];

  const coin1 = {
    x: grid.matrix[7][5].x,
    y: grid.matrix[7][5].y,
  };

  coinsArr.push(coin1);

  const coin2 = {
    x: grid.matrix[8][5].x,
    y: grid.matrix[8][5].y,
  };

  coinsArr.push(coin2);

  const coin3 = {
    x: grid.matrix[9][5].x,
    y: grid.matrix[9][5].y,
  };

  coinsArr.push(coin3);

  const coin4 = {
    x: grid.matrix[7][6].x,
    y: grid.matrix[7][6].y,
  };

  coinsArr.push(coin4);

  const coin5 = {
    x: grid.matrix[8][6].x,
    y: grid.matrix[8][6].y,
  };

  coinsArr.push(coin5);

  const coin6 = {
    x: grid.matrix[9][6].x,
    y: grid.matrix[9][6].y,
  };

  coinsArr.push(coin6);

  const coin7 = {
    x: grid.matrix[7][7].x,
    y: grid.matrix[7][7].y,
  };

  coinsArr.push(coin7);

  const coin8 = {
    x: grid.matrix[8][7].x,
    y: grid.matrix[8][7].y,
  };

  coinsArr.push(coin8);

  const coin9 = {
    x: grid.matrix[9][7].x,
    y: grid.matrix[9][7].y,
  };

  coinsArr.push(coin9);

  const coin10 = {
    x: grid.matrix[4][9].x,
    y: grid.matrix[4][9].y,
  };

  coinsArr.push(coin10);

  const coin11 = {
    x: grid.matrix[5][9].x,
    y: grid.matrix[5][9].y,
  };

  coinsArr.push(coin11);

  const coin12 = {
    x: grid.matrix[6][9].x,
    y: grid.matrix[6][9].y,
  };

  coinsArr.push(coin12);

  const coin13 = {
    x: grid.matrix[4][10].x,
    y: grid.matrix[4][10].y,
  };

  coinsArr.push(coin13);

  const coin14 = {
    x: grid.matrix[5][10].x,
    y: grid.matrix[5][10].y,
  };

  coinsArr.push(coin14);

  const coin15 = {
    x: grid.matrix[6][10].x,
    y: grid.matrix[6][10].y,
  };

  coinsArr.push(coin15);

  const coin16 = {
    x: grid.matrix[4][11].x,
    y: grid.matrix[4][11].y,
  };

  coinsArr.push(coin16);

  const coin17 = {
    x: grid.matrix[5][11].x,
    y: grid.matrix[5][11].y,
  };

  coinsArr.push(coin17);

  const coin18 = {
    x: grid.matrix[6][11].x,
    y: grid.matrix[6][11].y,
  };

  coinsArr.push(coin18);

  const coin19 = {
    x: grid.matrix[10][12].x,
    y: grid.matrix[10][12].y,
  };

  coinsArr.push(coin19);

  const coin20 = {
    x: grid.matrix[11][12].x,
    y: grid.matrix[11][12].y,
  };

  coinsArr.push(coin20);

  const coin21 = {
    x: grid.matrix[12][12].x,
    y: grid.matrix[12][12].y,
  };

  coinsArr.push(coin21);

  const coin22 = {
    x: grid.matrix[10][13].x,
    y: grid.matrix[10][13].y,
  };

  coinsArr.push(coin22);

  const coin23 = {
    x: grid.matrix[11][13].x,
    y: grid.matrix[11][13].y,
  };

  coinsArr.push(coin23);

  const coin24 = {
    x: grid.matrix[12][13].x,
    y: grid.matrix[12][13].y,
  };

  coinsArr.push(coin24);

  const coin25 = {
    x: grid.matrix[10][14].x,
    y: grid.matrix[10][14].y,
  };

  coinsArr.push(coin25);

  const coin26 = {
    x: grid.matrix[11][14].x,
    y: grid.matrix[11][14].y,
  };

  coinsArr.push(coin26);

  const coin27 = {
    x: grid.matrix[12][14].x,
    y: grid.matrix[12][14].y,
  };

  coinsArr.push(coin27);

  return coinsArr;
}

export function GROUP3(grid) {
  const coinsArr = [];
  const arr1 = new Array(12).fill(0);

  arr1.forEach((item, idx) => {
    if (idx >= 6) {
      idx += 1;
    }
    const coin1 = {
      x: grid.matrix[7][2 + idx].x,
      y: grid.matrix[7][2 + idx].y,
    };

    coinsArr.push(coin1);

    const coin2 = {
      x: grid.matrix[8][2 + idx].x,
      y: grid.matrix[8][2 + idx].y,
    };

    coinsArr.push(coin2);

    const coin3 = {
      x: grid.matrix[9][2 + idx].x,
      y: grid.matrix[9][2 + idx].y,
    };

    coinsArr.push(coin3);
  });

  return coinsArr;
}

export function GROUP4(grid) {
  const coinsArr = [];
  const arr1 = new Array(4).fill(0);
  let row;

  arr1.forEach((item, idx) => {
    switch (idx) {
      case 0:
      case 2:
        row = 4;
        break;
      case 1:
      case 3:
        row = 12;
        break;
      default:
        break;
    }

    const coin1 = {
      x: grid.matrix[row][2 + idx * 4].x,
      y: grid.matrix[row][2 + idx * 4].y,
    };

    coinsArr.push(coin1);

    const coin2 = {
      x: grid.matrix[row + 1][2 + idx * 4].x,
      y: grid.matrix[row + 1][2 + idx * 4].y,
    };

    coinsArr.push(coin2);

    const coin3 = {
      x: grid.matrix[row + 2][2 + idx * 4].x,
      y: grid.matrix[row + 2][2 + idx * 4].y,
    };

    coinsArr.push(coin3);

    const coin4 = {
      x: grid.matrix[row][3 + idx * 4].x,
      y: grid.matrix[row][3 + idx].y,
    };

    coinsArr.push(coin4);

    const coin5 = {
      x: grid.matrix[row + 1][3 + idx * 4].x,
      y: grid.matrix[row + 1][3 + idx * 4].y,
    };

    coinsArr.push(coin5);

    const coin6 = {
      x: grid.matrix[row + 2][3 + idx * 4].x,
      y: grid.matrix[row + 2][3 + idx * 4].y,
    };

    coinsArr.push(coin6);
  });

  return coinsArr;
}

export function GROUP5(grid) {
  const coinsArr = [];
  const arrRow = new Array(3).fill(0);
  const arrCol = new Array(6).fill(0);

  arrRow.forEach((rowItem, rowIdx) => {
    let colOffset = rowIdx;
    arrCol.forEach((colItem, colIdx) => {
      const coin1 = {
        x: grid.matrix[rowIdx + 4][colIdx + colOffset + 2].x,
        y: grid.matrix[rowIdx + 4][colIdx + colOffset + 2].y,
      };

      coinsArr.push(coin1);
    });
  });
  return coinsArr;
}

export function GROUP6(grid) {
  const coinsArr = [];
  const arrRow = new Array(3).fill(0);
  const arrCol = new Array(6).fill(0);

  arrRow.forEach((rowItem, rowIdx) => {
    let colOffset = arrRow.length - rowIdx;
    arrCol.forEach((colItem, colIdx) => {
      const coin1 = {
        x: grid.matrix[rowIdx + 12][colIdx + colOffset + 2].x,
        y: grid.matrix[rowIdx + 12][colIdx + colOffset + 2].y,
      };

      coinsArr.push(coin1);
    });
  });
  return coinsArr;
}

export function GROUP7(grid) {
  const coinsArr = [];
  const arrRow = new Array(3).fill(0);
  const arrCol = new Array(6).fill(0);

  arrRow.forEach((rowItem, rowIdx) => {
    arrCol.forEach((colItem, colIdx) => {
      const coin1 = {
        x: grid.matrix[rowIdx * 6 + 2][colIdx + 2].x,
        y: grid.matrix[rowIdx * 6 + 2][colIdx + 2].y,
      };

      coinsArr.push(coin1);
    });
  });
  return coinsArr;
}
